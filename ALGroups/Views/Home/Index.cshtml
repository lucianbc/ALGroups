@model ICollection<ALGroups.ViewModels.GroupCard>
@{
    ViewBag.Title = "Home Page";
}

<div class="row">
    <div class="col-lg-12">
        <h2>Latest added groups...</h2>
    </div>
</div>
<div class="row mt-3">
    @foreach (var x in Model) 
    {
        @Html.Partial("_GroupCard", @x)
    }
</div>
